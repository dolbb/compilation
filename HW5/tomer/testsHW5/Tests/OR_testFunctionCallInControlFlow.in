void myPrint(int i){
	print("got ");
	printi(i);
	print("\n");
}
byte myPrint2(int i){
	print("got ");
	printi(i);
	print("\n");
	return 1b;
}
void main()
{
	int x = 1;
	byte b1;
	if(true)
		myPrint(x);
	if(true)
		byte b2 = myPrint2(x+1);
	if(true)
		b1 = myPrint2(x+2);

	x = x + 3;//4
	if(true)
		{myPrint(x);}
	if(true)
		{byte b3 = myPrint2(x+1);}
	if(true)
		{b1 = myPrint2(x+2);}

	x = x + 3;//7
	if(true)
		myPrint(x);
	else
		int t;
	if(true)
		byte b4 = myPrint2(x+1);
	else
		int t;
	if(true)
		b1 = myPrint2(x+2);
	else
		int t;

	x = x + 3;//10
	if(true)
		{myPrint(x);}
	else
		int t;
	if(true)
		{byte b5 = myPrint2(x+1);}
	else
		int t;
	if(true)
		{b1 = myPrint2(x+2);}
	else
		int t;

	x = x + 3;//13
	if(false)
		int t;
	else
		myPrint(x);
	if(false)
		int t;
	else
		byte b6 = myPrint2(x+1);
	if(false)
		int t;
	else
		b1 = myPrint2(x+2);

	x = x + 3;//16
	if(false)
		int t;
	else
		{myPrint(x);}
	if(false)
		int t;
	else
		{byte b7 = myPrint2(x+1);}
	if(false)
		int t;
	else
		{b1 = myPrint2(x+2);}
	b1 = 0b;
	x = x + 3;//19
	while(b1 == 0)
		b1 = myPrint2(x);
	b1 = 0b;
	while(b1 == 0)
		{b1 = myPrint2(x+1);}
	while(true)
	{
		myPrint(x+2);
		break;
	}
	while(b1 == 1b)
	{
		byte b69 = myPrint2(x+3);
		b1 = b1 - b69;
	}
}