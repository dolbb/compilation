int secret(int x[250],int y[70], byte z,bool w[128]){	int h[200];	bool m[1]; // Size 1 is accepted :)	byte j[200];	{		{			{				byte magic[6];				int help = 8;			}			{				byte magic[5];				int wild = 666;			}			bool help = m[0];		}		int magic[70];		magic[magic[3]] = magic[3];		byte help[69];		int gold[1];	}	byte magic[42];	return magic[21];}void main(){	bool magic[255];	return;}// Don't try to understand code logic...// The test is about scoping and forgetting (Variable !!magic!!)// Magic should be forgotten when we exit a scope, any scope!